// Stub for CustomizedDataGrid - @mui/x-data-grid not installed

import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export default function CustomizedDataGrid() {
  return (
    <Box sx={{ p: 2, bgcolor: "#fafafa", borderRadius: 1 }}>
      <Typography>DataGrid Not Available</Typography>
      <Typography variant="body2">@mui/x-data-grid is not installed</Typography>
    </Box>
  );
}
