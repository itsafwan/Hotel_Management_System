// Stub for ChartUserByCountry - @mui/x-charts not installed
import Typography from "@mui/material/Typography";
import Card from "@mui/material/Card";
import CardContent from "@mui/material/CardContent";

export default function ChartUserByCountry() {
  return (
    <Card variant="outlined" sx={{ width: "100%" }}>
      <CardContent>
        <Typography>Users by country chart not available</Typography>
        <Typography variant="body2">@mui/x-charts is not installed</Typography>
      </CardContent>
    </Card>
  );
}
